module.exports = async (msg = "[continue]") => {
  // process.stdin.setRawMode(true);
  // console.log("\n" + msg);
  // return new Promise(resolve =>
  //   process.stdin.once("data", () => {
  //     process.stdin.setRawMode(false);
  //     resolve();
  //   })
  // );
};
